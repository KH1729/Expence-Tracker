import { ExpenseDashboard } from '@/components/ExpenseDashboard';

/**
 * @description Home route: expense dashboard (client UI in `ExpenseDashboard`).
 */
export default function Home() {
  return <ExpenseDashboard />;
}

/**
 * @description Distinct category names used by server `MOCK_EXPENSE_SEED_ROWS` (sorted).
 * Keep in sync with `server/src/db/seedMockExpenses.ts`.
 */
export const MOCK_SEED_CATEGORY_NAMES: readonly string[] = [
  'Food',
  'Home',
  'Subscriptions',
  'Transport',
];
